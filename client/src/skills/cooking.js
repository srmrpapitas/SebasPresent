/**
 * SebasPresent — Cooking skill (placeholder S32)
 *
 * Va a implementar cocinar raw food en fires.
 * Patrón: tap en fire con raw_X en inv → check level → consume raw_X + delete fire
 * (o no, según diseño) → da cooked_X + XP.
 *
 * Para implementar: copiar estructura de woodcutting.js o firemaking.js.
 * Ver `skills/README.md` para los pasos completos.
 *
 * Status: NO IMPLEMENTADO. Las funciones son no-op.
 */

export function start(_opts) {
  // no-op (placeholder)
}

export function stop() {
  // no-op
}

export function update(_dt) {
  // no-op
}

export function cancelOnMove() {
  // no-op
}
