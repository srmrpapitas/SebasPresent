/**
 * SebasPresent — Mining skill (placeholder S33)
 *
 * Va a implementar picar rocks con pickaxe equipada.
 * Patrón: tap en rock → check level + tener pickaxe → loop tick →
 * da ore + XP. Rocks depleted con respawn.
 *
 * Para implementar: copiar estructura de woodcutting.js (que es muy similar:
 * tap on entity → walk → tick loop → resource + XP + depleted state).
 *
 * Ver `skills/README.md` para los pasos completos.
 *
 * Status: NO IMPLEMENTADO. Las funciones son no-op.
 */

export function start(_opts) {
  // no-op (placeholder)
}

export function stop() {
  // no-op
}

export function update(_dt) {
  // no-op
}

export function cancelOnMove() {
  // no-op
}
