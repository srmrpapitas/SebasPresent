/**
 * SebasPresent — Runtime inspector (Sesión 31, FASE 2 — placeholder)
 *
 * Objetivo (futuro): introspección on-demand del estado del juego sin
 * tener que recordar nombres de hooks. Idea: una función global
 *
 *   window.__inspect(thing)
 *
 * que reciba 'character', 'world', 'snapshot', 'equipment', 'skill:woodcutting'
 * y devuelva un dump estructurado con TODO lo relevante de ese subsistema.
 *
 * Por ahora no está implementado. Las funciones equivalentes viven en
 * `diag.js` (dumpCharacterState, printSnapshot, printEquipment).
 *
 * Cuando se haga, registrar window.__inspect aquí.
 */

export function installInspector() {
  // No-op por ahora.
}
